AI-native runtime routing for aiforbn.

HUMAN_DOCS_POLICY=user_owned_read_only_unless_explicit_human_document_task
human_docs/ is user-owned context, not agent state; changes require an explicit task for those documents.

Start with AGENTS.md and HANDOFF.md. Use the nearest module instructions/API summary for implementation details; COMMON_FUNCTIONS.md routes shared helpers, TESTING.md explains execution, and SERVICES.md describes the optional viewer.

First inspection: python3 main.py --verify-agent-contract
Command index: python3 main.py --emit-agent-commands
Run both with the verified quant interpreter (see TESTING.md).

Select from the emitted profiles:
- `architecture_doc_skill_edit`: documentation, skills, handoff, manifest.
- `module_logic_edit`: public API, dependency, model or module logic.
- `scientific_pipeline_edit`: scientific pipeline/artifact behavior; recompute only when the task needs fresh outputs.
- `ui_edit`: UI/rendering; bounded HTTP startup checks are useful when launch wiring changes.
The manifest's requires/provides fields define coverage; the smallest sufficient profile is normally enough.

Preserve formula-only screening, grouped evaluation, and the distinction between ranking, unrelaxed prototypes, and physical validation. Use provenance to assess artifact currentness.
Keep main.py traceable and cross-module calls on documented public APIs. Reuse helpers where compatible without discarding project-specific guards.
The repo-scoped workflow skill carries maintenance decisions; the proposal skill applies to authorized research-plan/Overleaf work. Legacy guidance files listed as retired in the manifest remain retired.
